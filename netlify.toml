[build]
  publish = "."
  functions = "netlify/functions"
  command = "npm install && node scripts/prepare-wallet.js"

[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/oracle-data-wallet/**"]
